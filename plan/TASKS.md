# Suivi des tâches — Arborisis V2

Fichier de suivi vivant, à cocher au fur et à mesure. Reflète le phasage de [11-roadmap.md](11-roadmap.md).
Légende statut : `☐` à faire · `▶` en cours · `☑` fait · `⛔` bloqué (raison en note)

Dernière mise à jour : 2026-08-20 (upload et prévisualisation audio cassés en prod le lendemain du lancement — corrigés et déployés, voir journal. Génération de la carte de production **Europe** en cours sur `arborisis-photon-1`, pas encore terminée. Arborisis reste en ligne : https://arborisis.com sert l'application réelle, premier déploiement complet sur `arborisis-app-1`. Login/Register réimplémentés contre le design system. Phase 5 close : audit accessibilité, modération, sauvegardes testées en restauration réelle, pages légales, revue de sécurité (0 vulnérabilité), géocodage Photon planète + tuiles auto-hébergés)

---

## Phase 0 — Bootstrap infra (avant tout code produit)

| Tâche | Statut | Note |
|---|---|---|
| Créer l'organisation GitHub, dépôt public | ☑ | Dépôt public créé sous le compte `0xmagicduck` : [github.com/0xmagicduck/arborisis](https://github.com/0xmagicduck/arborisis) (pas d'organisation GitHub séparée, dépôt personnel) |
| Licence AGPL-3.0 + `CONTRIBUTING.md`/`CODE_OF_CONDUCT.md`/`SECURITY.md` | ☑ | Fichiers créés et poussés sur le dépôt |
| Provisionner le domaine `arborisis.com` (DNS) | ☑ | Domaine déjà possédé par l'utilisateur (registrar Gandi, DNS géré chez Cloudflare). Enregistrement `A` déjà présent et pointant vers l'IP flottante Infomaniak — aucune action DNS nécessaire |
| Provisionner la VM Infomaniak Public Cloud + Object Storage via Terraform | ☑ | `terraform apply` exécuté sur `dc3-a` : VM `arborisis-app-1` (4vCPU/8Go/80Go, ~16,84€/mois), IP flottante `195.15.247.170`, réseau/secgroup dédiés, container Object Storage `arborisis-storage` (privé) |
| Caddy fonctionnel avec HTTPS auto sur une page "hello world" | ☑ | **Validé en production** : [https://arborisis.com](https://arborisis.com) répond en HTTP/2 200 avec certificat Let's Encrypt valide |
| Demander la création de la collection Internet Archive dédiée | ⛔ | **Bloqué** : Internet Archive n'examine une demande de collection dédiée qu'après ≈50 items déjà publiés par le compte — seuil inatteignable avant lancement. Reporté, voir [05.10](05-stockage-audio-internet-archive.md#510-mode-intérimaire--repli-sur-object-storage-infomaniak-pas-dinternet-archive-au-démarrage). Pas d'action possible tant qu'Arborisis n'a pas d'utilisateurs réels. |
| Créer les comptes/clés nécessaires (Internet Archive IAS3, Object Storage Infomaniak) | ▶ | Identifiants OpenStack Infomaniak configurés en local (`~/.config/openstack/clouds.yaml`), non commités, VM provisionnée avec succès. IAS3 (Internet Archive) **volontairement non créé pour l'instant** — voir mode intérimaire ci-dessus ; tout repose sur l'Object Storage Infomaniak déjà provisionné. |

## Phase 1 — Socle technique

| Tâche | Statut | Note |
|---|---|---|
| Scaffold monorepo (pnpm + Turborepo), packages `shared-types`, `db`, `design-tokens` | ☑ | `apps/{web,api,worker}` + `packages/{shared-types,db,design-tokens}`. Vérifié réellement : `pnpm install`, `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` passent tous sur les 6 packages (pas seulement écrit — exécuté) |
| Schéma PostgreSQL/PostGIS + migrations (Drizzle) | ☑ | Schéma dans [packages/db/src/schema.ts](../packages/db/src/schema.ts) (users, webauthn_credentials, recovery_codes, recordings + index GIST). Migration **appliquée avec succès** sur un Postgres/PostGIS réel via `docker compose up` + `pnpm db:migrate` — deux bugs réels trouvés et corrigés à cette occasion (voir journal) : chemin de migration cassé par un espace dans le nom de dossier, et `drizzle-kit generate` qui quote à tort le type `geography(Point,4326)` (absent de sa liste de types natifs) |
| Authentification WebAuthn de bout en bout | ☑ | Code complet : `apps/api/src/routes/auth.ts` (register/login start+finish, sessions Redis révocables, codes de récupération, rate limiting) + `apps/web/app/{register,login}` (`@simplewebauthn/browser`). **Cérémonie complète testée en conditions réelles** dans le navigateur (inscription → 10 codes de récupération affichés → déconnexion implicite → connexion → session persistée, vérifié via `/auth/me` et directement en base) contre Postgres/Redis vivants. Un bug réel corrigé au passage : `setErrorHandler` posé après l'enregistrement des routes ne s'appliquait pas (encapsulation Fastify) et laissait fuiter les erreurs Zod brutes en 500 au lieu d'un 400 propre |
| CI de base (lint, typecheck, tests) | ☑ | [.github/workflows/ci.yml](../.github/workflows/ci.yml) — mêmes commandes que celles vérifiées en local ci-dessus, plus Postgres/Redis en services GitHub Actions |

## Phase 2 — Pipeline d'upload et archivage

| Tâche | Statut | Note |
|---|---|---|
| Upload direct vers Object Storage (URL pré-signées) | ☑ | `POST /uploads/presign` (staging TTL 15 min, quota `MAX_UPLOAD_BYTES`) + `POST /recordings` (vérifie le dépôt via `HeadObject` avant de créer l'enregistrement). Nouveaux packages `@arborisis/storage` (client S3-compat + presign) et `@arborisis/queue` (BullMQ partagé api/worker). **Testé en conditions réelles contre l'Object Storage Infomaniak réel** (pas seulement MinIO), voir journal |
| Worker : validation ffprobe, transcodage ffmpeg, waveform peaks | ☑ | `apps/worker/src/jobs/publish-recording.ts` + `apps/worker/src/lib/audio.ts` (ffmpeg/ffprobe système, pas de binaire npm static). Proxy en Opus 128kbps (tranché §5.9). Peaks calculés depuis un décodage PCM brut. Tests réels contre ffmpeg (pas de mock) dans `apps/worker/src/__tests__/audio.test.ts`, CI mise à jour pour installer ffmpeg |
| Intégration Internet Archive (push IAS3, polling, retry) | ⛔ | Reportée après lancement (seuil ≈50 items), voir [05.10](05-stockage-audio-internet-archive.md#510-mode-intérimaire--repli-sur-object-storage-infomaniak-pas-dinternet-archive-au-démarrage). Le worker garde la structure du job, étape IA en no-op derrière `ARCHIVE_TO_IA=false` |
| État `processing` visible côté utilisateur | ☑ | `GET /recordings/:id` (public une fois `published`, sinon réservé à l'auteur — pas de fuite d'existence) et `GET /recordings/mine`. Échec définitif après épuisement des retries BullMQ → `status = 'failed'` (voir §5.6), testé en conditions réelles |

## Phase 3 — Écrans du MVP (frontend)

| Tâche | Statut | Note |
|---|---|---|
| Explorer | ☑ | `app/page.tsx` — fidèle à `design/system/Explorer.dc.html`. Carte = placeholder SVG topographique (voir `components/ExplorerMap.tsx`) : projection équirectangulaire réelle des coordonnées, mais **pas** la vraie carte interactive (MapLibre/pmtiles/clustering = Phase 4, explicitement documenté dans le composant). Panneau de sélection + bande "Selected" (fallback accessible) vérifiés en conditions réelles |
| Découvrir | ☑ | `app/decouvrir/page.tsx` — `design/system/Discover.dc.html`. Pas de champ `featured` en base : l'entrée mise en avant est la plus récente plutôt qu'un algorithme inventé (voir note plus bas) |
| Recording Detail | ☑ | `app/enregistrements/[id]/page.tsx` — `design/system/RecordingDetail.dc.html`. Mini-carte cliquable → Explorer recentré (`/?focus=<id>`, vérifié) |
| Ajouter (3 étapes) | ☑ | `app/ajouter/{page,StepSound,StepDetails,StepPublish}.tsx` — `design/system/Upload1-3.dc.html`. **Cycle complet exécuté réellement dans le navigateur** : presign → upload MinIO → création d'enregistrement → job enqueue → redirection vers la fiche publiée |
| Profil | ☑ | `app/profil/page.tsx` — `design/system/Profile.dc.html`. Le sien uniquement (pas de profil public d'autrui, hors brief MVP). Affiche l'état `processing`/`failed` des enregistrements de l'auteur (« Archiving… ») |
| Recherche | ☑ | `app/recherche/page.tsx` — `design/system/Search.dc.html`. `q` tape dans un `ILIKE` naïf sur titre/lieu/tags côté `GET /recordings` (voir note plus bas) : pis-aller assumé avant Meilisearch (Phase 4). Filtres "location/tag/duration" du mockup affichés en texte inerte (pas de contrepartie serveur pour l'instant) plutôt qu'en liens qui ne feraient rien |
| Déclinaisons mobile de chaque écran | ☑ | Pas de composants "Mobile*" séparés : un même composant par écran, responsive fluide en CSS (media queries, breakpoint 768px) — cohérent avec DEV-HANDOFF §3.7/§4 ("doivent être fluides, pas figées à cette largeur"). Vérifié visuellement à 390px et desktop pour les 6 écrans |
| **Login / Register — vrai front-end** | ☑ | `apps/web/app/{login,register}/page.tsx` réimplémentés contre les composants partagés (`Button`, `FormField`) et un nouveau gabarit `components/AuthPage.{tsx,module.css}` — même patron que `LegalPage` (colonne étroite centrée, titre serif italique, retour "← Explorer") puisqu'aucun mockup `.dc.html` n'existe pour ces deux écrans, comme documenté ci-dessous. Logique WebAuthn inchangée (déjà validée Phase 1), juste ajout de `refresh()` de session après connexion/inscription. **Vérifié en conditions réelles dans le navigateur** contre l'API/Postgres/Redis réels (pas MinIO/mocks) : inscription complète (pseudo → passkey → 10 codes de récupération affichés en grille 2 colonnes → redirection Explorer authentifié), déconnexion, puis reconnexion avec le même compte — les deux cycles aboutissent avec l'avatar de header qui bascule correctement vers l'état connecté. Compte de test supprimé de la base après vérification. Desktop (1280px) et mobile (rendu natif < 768px, header "back") comparés visuellement aux autres écrans de l'app. `tsc --noEmit` et `eslint` passent sans erreur. |

**Décisions prises pendant cette phase (à connaître avant de retoucher ces écrans) :**
- **`GET /recordings` (liste publique)** créée — n'existait pas avant cette phase. Jointure sur `users` ajoutée à toutes les requêtes de lecture (`authorHandle`/`authorDisplayName` dans `recordingSchema`) pour afficher "Recorded by {auteur}" sans aller-retour supplémentaire. Voir `apps/api/src/routes/recordings.ts`.
- **Géocodage non disponible en Phase 3** (Photon = Phase 4, voir plan/07-carte-open-source.md §7.5) : l'étape 2 du flux Ajouter capture les coordonnées via la géolocalisation navigateur (`navigator.geolocation`, bouton "Use my current location") pendant que l'utilisateur·ice décrit le lieu en texte libre, plutôt que de bloquer tout le flux d'upload en attendant l'intégration Photon. À remplacer par un vrai autocomplete géographique en Phase 4 — voir `apps/web/app/ajouter/StepDetails.tsx`.
- **`BottomTabBar` : toujours les 4 mêmes items (Explorer/Découvrir/Ajouter/Recherche), jamais un 5ᵉ pour Profil** — suit la règle explicite du handoff (§2.8) plutôt que `MobileProfile.dc.html`, qui dessine à la place un badge avatar en 4ᵉ position (incohérence entre ce mockup précis et le composant tel que documenté en prose ailleurs). Profil, comme RecordingDetail et Ajouter, a un header mobile "back" et pas de barre d'onglets — voir `components/BottomTabBar.tsx`.
- **Pas de date de prise de son dans le flux Ajouter** (`recordedAt` requis par le schéma mais aucun champ dans les mockups Upload2/3) : défaut à la date d'upload (`new Date()`), documenté comme simplification assumée plutôt qu'un champ de formulaire inventé hors design.
- Recherche/Profil-d'autrui/pagination avancée : non couverts (pas dans le brief MVP à 6 écrans, voir `design/README.md`).
- **Login/Register** : traités dans une session séparée (voir journal), aucun mockup n'existant pour ces deux écrans dans `design/system/` — gabarit dérivé des tokens/composants existants (`AuthPage`, même patron que `LegalPage`) plutôt que designé à la volée hors du process habituel (mockup → implémentation).

## Phase 4 — Carte et recherche

| Tâche | Statut | Note |
|---|---|---|
| Premier fichier `.pmtiles` (Planetiler) | ☑ | Fichier réel généré (extrait Luxembourg, ~36 Mo, profil OpenMapTiles — pas le schéma Protomaps, voir note plus bas) via le pipeline reproductible `infra/tiles/generate.sh` (documenté dans `infra/tiles/README.md`). Servi en dev same-origin par Next.js (`apps/web/public/tiles/`), requêtes `Range` HTTP confirmées fonctionnelles (statut `206 Partial Content`) dans le navigateur. Pas encore déposé sur un Object Storage de production — l'emprise géographique de production reste "à trancher" (§7.7), ce Luxembourg n'est qu'une validation de bout en bout du pipeline |
| Intégration MapLibre + style "Quiet Cartography" | ☑ | `apps/web/map-style/quiet-cartography.ts` + `components/ExplorerMap.tsx`. **Vérifié dans un vrai navigateur** : fond de carte réel (routes/bâti du Luxembourg rendus en traits fins monochromes conformes au style), attribution "© OpenMapTiles © OpenStreetMap contributors" visible (licence CC-BY, obligatoire) |
| Clustering client (Supercluster), requêtes viewport PostGIS | ☑ | Marqueurs `maplibregl.Marker` HTML pilotés par une vraie instance Supercluster, alimentée par `GET /recordings/viewport` (`ST_MakeEnvelope` + `&&` sur l'index GIST). **Vérifié dans le navigateur** : cluster "3" affiché pour 3 enregistrements proches (Luxembourg City), clic → zoom in → re-clustering en "2" + 1 marqueur individuel, clic sur un marqueur individuel → panneau de sélection peuplé avec les vraies données (titre, lieu, auteur, durée) via `GET /recordings/:id` |
| Géocodage (Photon, bootstrap → auto-hébergé) | ☑ | Autocomplete câblé dans `StepDetails.tsx` (étape 2 Ajouter). CORS + requête réelle vérifiés directement depuis l'origine `localhost:3000` (résultats Photon réels reçus, y compris "Forêt de Soignes/Sonian Forest, Belgique"). Bootstrap uniquement — auto-hébergement resté hors scope de cette session |
| Indexation Meilisearch + écran Recherche | ☑ | Nouveau package `@arborisis/search`, indexation à la publication dans `apps/worker`, `GET /recordings/search` avec facettes (tags/license/locationLabel). **Vérifié dans le navigateur** : menu déroulant "tag" listant les facettes réelles (birds/forest/wind/dawn/insects/night/stream/urban avec comptages), filtre "birds" appliqué → "2 recordings found" avec les bons résultats (Dawn chorus, Sonian Forest) |

**Non fait dans cette session** : dépôt du `.pmtiles` sur un Object Storage réel (reste local/dev), auto-hébergement Photon (reste bootstrap public), toute décision sur l'emprise géographique de production, vérification du flux Ajouter complet avec upload réel (testé uniquement isolément : Photon en JS console, pas le composant `StepDetails` lui-même dans son formulaire — nécessiterait une session WebAuthn ou une injection Redis comme en Phase 3).

**Incident d'environnement rencontré et contourné en session** (voir aussi [plan/MEMORY.md](MEMORY.md) § Infra) : le dépôt vit dans iCloud Drive ; un reboot de la machine pendant la session a laissé `fileproviderd` saturer l'I/O (70-150% CPU en continu sur plus d'une heure), rendant `node_modules` intermittemment illisible — `npx tsx src/index.ts` plantait avec une erreur interne (fichier `tsx` lu vide/tronqué), et même des commandes triviales (`tsc --noEmit`, `eslint`, `grep -r`) prenaient plusieurs dizaines de minutes au lieu de secondes. Contourné en basculant `apps/api`/`apps/worker` sur un lancement `node dist/index.js` (build `tsc` classique) plutôt que `tsx` (qui passe par esbuild, la couche qui coinçait) — une fois lancés ainsi, tout a répondu normalement. L'utilisateur a aussi basculé le dossier en "toujours garder sur ce Mac" en cours de session.

**Bugs réels trouvés et corrigés pendant la vérification** :
- `pnpm typecheck` : décoration Fastify `search` en collision de type avec la méthode HTTP native `fastify.search()` (renommée `fastify.meilisearch`) ; deux cas `noUncheckedIndexedAccess` dans `ExplorerMap.tsx` (déstructuration de `Position` GeoJSON, assignation de classe CSS Modules à `element.className` natif). Détail dans plan/MEMORY.md.
- `next build` échouait sur `Html should not be imported outside of pages/_document` — message sans rapport apparent avec la vraie cause (`maplibre-gl` importé statiquement fait planter le rendu SSR que Next tente même pour une page `"use client"` lors de la génération statique). Corrigé en import dynamique `next/dynamic({ ssr: false })` pour `ExplorerMap` dans `apps/web/app/page.tsx`. Confirmé responsable en comparant un build avant/après `git stash` des changements Phase 4 (Phase 3 seule buildait proprement).
- Carte confinée à ~67px de hauteur au lieu de remplir tout `.mapArea` : `height: 100%` sur un enfant statique d'un conteneur dont la hauteur vient d'un `flex: 1` est ambigu selon les moteurs de rendu. Corrigé en `position: absolute; inset: 0` contre l'ancêtre `position: relative`, voir `ExplorerMap.module.css`.

## Phase 5 — Durcissement avant lancement

| Tâche | Statut | Note |
|---|---|---|
| Audit accessibilité complet (WCAG AA) | ☑ | Contrastes `--color-stone` (~3.3:1) corrigés en `--color-ink-secondary` (~6.6:1) partout où le texte porte une information unique et non redondante (nav desktop, back-links, auteur/lieu, compteurs, liens fonctionnels, labels de formulaire) — durées laissées en l'état (`--color-ink-faint`, décision produit documentée dans les tokens). Cibles tactiles 44px ajoutées sur les radiogroups (licence, signalement). Vérifié dans un vrai navigateur (rendu + ordre de tabulation) après un incident HMR Next.js contourné par un restart propre. Non refait : audit outillé (axe-core) sur l'ensemble des écrans — fait manuellement contre la checklist DEV-HANDOFF §7 |
| Politique de modération minimale (signalement) | ☑ | Bouton "Report this recording" sur RecordingDetail → `POST /recordings/:id/reports` (auth requise, idempotent, rate-limité). Revue via `GET`/`PATCH /reports`, restreinte aux handles `ADMIN_HANDLES` (pas de rôle admin en base, pas d'écran dédié — simplification assumée, voir `apps/api/src/lib/admin.ts`). **Vérifié en conditions réelles** : signalement déposé dans le navigateur (non-connecté → message + lien login ; connecté → succès), lu et résolu via l'API en tant qu'admin, rejeté en 403 pour un non-admin |
| Sauvegardes automatisées + test de restauration | ☑ | Nouveau package `@arborisis/backup` (`pg_dump` custom-format → chiffrement GPG AES256 → dépôt Object Storage, rétention 30j glissants + 12 mensuelles). **Testé en conditions réelles contre l'Object Storage Infomaniak réel** (`arborisis-storage`, pas MinIO) : sauvegarde réelle déposée, restaurée dans une base PostgreSQL/PostGIS fraîche, comptages de lignes identiques à la source. Pas encore câblé dans `infra/docker-compose.yml` (l'app n'est pas encore déployée en prod, voir Phase 6) — service systemd documenté dans `infra/backup/README.md`, prêt à l'activation |
| Politique de confidentialité, CGU, mentions légales (RGPD/nLPD) | ☑ | 3 pages réelles (`/mentions-legales`, `/confidentialite`, `/cgu`), identité réelle du responsable de traitement (fournie par l'utilisateur), reflètent l'état réel du traitement (pas d'email/mot de passe, IA pas encore active). Liées depuis Profil (pas de footer global — voir note dans le code, risque pour la carte Explorer). Aucun mockup design/system pour ces écrans (même situation que Login/Register) |
| Revue de sécurité (dépendances, headers HTTP, rate limiting auth) | ☑ | `pnpm audit` : 6 vulnérabilités (drizzle-orm SQLi direct + sharp/postcss transitifs via Next) toutes corrigées (`drizzle-orm`/`drizzle-kit` bump 0.36→0.45/0.31, overrides pnpm pour sharp/postcss) — **`0 vulnerabilities found`**, revérifié avec build/typecheck/test/migration réels après le bump. `@fastify/helmet` ajouté à l'API (headers vérifiés en conditions réelles). Caddyfile de prod (pas encore actif, voir Phase 6) enrichi d'en-têtes de sécurité + CSP, syntaxe validée via `caddy validate` réel. Rate limiting auth déjà en place depuis Phase 1 (confirmé, pas de régression) |
| Auto-hébergement définitif tuiles + géocodage | ☑ | **Géocodage** : VM dédiée `arborisis-photon-1` (32 Go RAM, réseau privé sans IP publique) + volume 300 Go, index **planète complet** Photon (90 Go) téléchargé et interrogé avec succès (Paris, Tokyo, Forêt de Soignes). Navigateur → `GET /geocode` (proxy API, `apps/api/src/routes/geocode.ts`) → réseau privé → Photon, plus jamais d'appel direct navigateur→Photon. **Tuiles** : `.pmtiles` Luxembourg déposé sur un container Object Storage public dédié (`arborisis-tiles`), Range requests confirmées, mais CORS Infomaniak indisponible (ACL Swift et `PutBucketCors` S3 testés, aucun ne fonctionne) → Caddy proxifie `/tiles/*` en same-origin (testé en conditions réelles avec une instance Caddy locale). Emprise géographique de production toujours Luxembourg (§7.7 toujours "à trancher") |

## Phase 6 — Lancement public

| Tâche | Statut | Note |
|---|---|---|
| Bascule DNS finale sur `arborisis.com` | ☑ | Le DNS pointait déjà vers `arborisis-app-1` depuis la Phase 0 — la « bascule » était en réalité le déploiement applicatif complet + le basculement de `infra/caddy/Caddyfile` du hello-world vers le bloc production (proxy `/api`, `/tiles`, `web`). **Premier déploiement réel de toute la stack** (postgres/redis/meilisearch/api/worker/web/backup) sur `arborisis-app-1`, voir `infra/docker/Dockerfile` (nouveau, 4 cibles) et `infra/docker-compose.yml`. **Vérifié en conditions réelles sur le domaine public** : `https://arborisis.com` sert l'app réelle (pas mock), migrations Drizzle appliquées contre le Postgres/PostGIS de prod, cycle d'inscription WebAuthn complet exécuté dans un vrai navigateur contre `https://arborisis.com/register` (passkey → 10 codes de récupération → session), `GET /api/recordings` et `GET /geocode` répondent réellement, tuiles `.pmtiles` servies via le proxy Caddy same-origin (`206 Partial Content` confirmé), sauvegarde nocturne testée en exécution manuelle réelle (`pg_dump` → GPG → dépôt Object Storage) et timer systemd activé. Compte de test supprimé après vérification. **Quatre bugs réels trouvés et corrigés pendant ce déploiement** (voir journal et `plan/MEMORY.md` § Déploiement/Docker) : mot de passe Postgres non URL-safe cassant la connection string, `MEILI_URL` manquant sur le service `worker` (crash-loop), `NEXT_PUBLIC_*` non inlinées dans le bundle web (jamais passées en build-arg Docker), et surtout **CSP `script-src 'self'` cassant le rendu de toute page** (page blanche en prod, jamais testée en conditions réelles avant cette session) — corrigé en urgence avec `'unsafe-inline'`, une CSP à base de nonce reste à faire proprement. |
| Annonce du dépôt GitHub ouvert aux contributions | ☐ | Pas faite cette session — action de publication distincte, à confirmer explicitement avec l'utilisateur avant d'écrire/publier une annonce (voir règles de permission de l'agent). |
| Surveillance queue Internet Archive (premières semaines) | ☐ | Sans objet tant que l'intégration IA reste reportée (mode intérimaire, voir plan/05 §5.10) — rien à surveiller. |
| Correctifs post-lancement : upload cassé (CORS) + prévisualisation audio cassée (CSP) | ☑ | Deux bugs réels remontés par l'utilisateur en conditions réelles le lendemain du lancement : (1) `URL.createObjectURL` (prévisualisation locale avant upload, `StepDetails.tsx`) bloqué par `media-src` sans `blob:` ; (2) tout `PUT` pré-signé vers l'Object Storage échouait en `405` au préflight CORS (passerelle S3-compat Infomaniak, jamais fiable pour CORS — déjà rencontré en Phase 5 pour les tuiles). Corrigés dans [PR #11](https://github.com/0xmagicduck/arborisis/pull/11) : `media-src` ajoute `blob:` ; upload proxifié same-origin via Caddy (`handle_path /storage-upload/*`, `OBJECT_STORAGE_UPLOAD_PROXY_URL`), même stratégie que `/tiles/*`. **Déployé et vérifié en conditions réelles sur `arborisis-app-1`** : `git pull` + rebuild `api`/`web`, CSP re-vérifiée via `curl -I` en prod. Bug de déploiement supplémentaire trouvé au passage : `docker compose exec caddy caddy reload` rechargeait l'ancien Caddyfile (bind mount lié à l'ancien inode après le `git pull`) — corrigé avec `docker compose up -d --force-recreate caddy`, voir `plan/MEMORY.md` § Déploiement/Docker. |
| Emprise géographique de production du `.pmtiles` (§7.7, était "à trancher") | ▶ | Décidé avec l'utilisateur : **Europe** (pas la planète entière — coût/temps jugés disproportionnés pour un lancement, voir échange en session ; pas un extrait plus modeste non plus, la communauté visée dépasse le Luxembourg). Génération Planetiler lancée en tâche de fond sur `arborisis-photon-1` (32 Go RAM, 190 Go libres sur `/mnt/photon-data` — VM existante réutilisée, aucun nouveau coût d'infra) plutôt que sur `arborisis-app-1` (RAM insuffisante, risquerait le site en prod) ou une VM temporaire (coût/complexité écartés par l'utilisateur). `-Xmx16g` + `nice`/`ionice` pour ne pas affamer le service Photon en prod qui tourne en parallèle sur la même VM. **Encore en cours à la fin de cette session** — voir "Prochaine session" pour la suite (dépôt sur Object Storage, bascule `NEXT_PUBLIC_PMTILES_URL`, rebuild `web`). |

---

## Décisions en attente (bloquent une action concrète)

- **Collection Internet Archive dédiée** : ⛔ bloquée structurellement, pas juste "lente" — IA exige ≈50 items déjà publiés avant d'examiner une demande de collection dédiée, voir [05.5](05-stockage-audio-internet-archive.md#55-licence--condition-dhébergement-pas-juste-une-option-ui). Rien à démarrer ici avant d'avoir des utilisateurs réels.
- **Compte IAS3** (Internet Archive) : reporté, plus nécessaire pour la Phase 2 — voir mode intérimaire [05.10](05-stockage-audio-internet-archive.md#510-mode-intérimaire--repli-sur-object-storage-infomaniak-pas-dinternet-archive-au-démarrage).
- **Reverse-proxy/CDN externe devant Caddy** (ex. Cloudflare) : le DNS `arborisis.com` est déjà chez Cloudflare — décider si on active leur proxy (orange cloud) ou si on reste en DNS-only comme recommandé par défaut dans [04.7](04-infra-infomaniak.md#47-à-trancher). Actuellement en DNS-only (le certificat Let's Encrypt de Caddy n'aurait pas pu s'émettre sinon).

## Décisions déjà tranchées cette session

- **GitHub** : dépôt public sous le compte personnel `0xmagicduck` (pas d'organisation séparée) — [github.com/0xmagicduck/arborisis](https://github.com/0xmagicduck/arborisis).
- **Domaine** : déjà possédé (registrar Gandi, DNS Cloudflare), déjà pointé vers l'IP flottante Infomaniak.
- **VM** : 4 vCPU / 8 Go RAM / 80 Go SSD, région `dc3-a`, budget cible 50-60€/mois (coût réel constaté : ~16,84€/mois pour la VM seule).
- **Géocodage Photon** : VM dédiée séparée de la VM de prod (`arborisis-photon-1`, 8 vCPU / 32 Go RAM / 20 Go disque racine) + volume Cinder 300 Go dédié aux données (`arborisis-photon-data`), plutôt qu'un redimensionnement du disque racine de la VM existante (aucun gabarit Infomaniak ne propose 300 Go de disque racine) ou qu'un partage avec la VM de prod (RAM insuffisante, risque pour le site en production). Coût mensuel exact non confirmé au moment de la rédaction — à vérifier dans le Manager Infomaniak (Phase 5, voir `infra/photon/README.md`).

## Prochaine session

**Arborisis est en ligne** (Phase 6, voir journal) — `https://arborisis.com` sert l'application réelle. **En premier à la prochaine session** : vérifier l'état de la génération `.pmtiles` Europe sur `arborisis-photon-1` (voir ci-dessous) — c'était toujours en cours à la fin de cette session-ci, plusieurs heures de traitement attendues.
1. **Finir le passage à la carte de production Europe** :
   - Vérifier `ssh ubuntu@195.15.245.101 -i ~/.ssh/arborisis_ed25519 "tail -f /mnt/photon-data/tiles-build/generate.log"` — la commande écrit `dist/europe.pmtiles` dans `/mnt/photon-data/tiles-build/` une fois finie (voir Phase 6 ci-dessus pour le contexte de la décision et les garde-fous RAM).
   - Une fois généré : déposer sur le container `arborisis-tiles` (déjà provisionné, public en lecture, voir infra/tiles/README.md § dépôt), remplacer `luxembourg.pmtiles` par `europe.pmtiles`.
   - Poser `NEXT_PUBLIC_PMTILES_URL=/tiles/europe.pmtiles` en build-arg (voir piège documenté dans `plan/MEMORY.md` § Déploiement/Docker : les `NEXT_PUBLIC_*` s'inlinent à la build, pas au runtime), rebuild+redeploy `web` sur `arborisis-app-1`.
   - Vérifier en navigateur réel (pas juste que le fichier se dépose) : zoom out jusqu'à voir l'échelle continentale, attribution OpenMapTiles/OSM toujours visible, clustering toujours correct sur les enregistrements existants.
   - Nettoyer `/mnt/photon-data/tiles-build/` sur `arborisis-photon-1` après vérification (le `.osm.pbf` de 25 Go et le `.pmtiles` final n'ont pas besoin de rester sur le volume de données Photon).
2. **Remplacer la CSP `'unsafe-inline'` par une CSP à base de nonce par requête** (middleware Next.js générant un nonce, propagé à l'en-tête `Content-Security-Policy` — plus possible en en-tête statique Caddy seul) — contournement d'urgence posé au premier déploiement, voir `plan/MEMORY.md` § Déploiement/Docker.
3. **Annonce du dépôt GitHub ouvert aux contributions** — action de publication distincte, à valider explicitement avec l'utilisateur avant d'écrire/publier quoi que ce soit (voir Phase 6 ci-dessus).
4. **Importer dans Terraform** les ressources créées en CLI (VM `arborisis-photon-1`, volume `arborisis-photon-data`, security group `arborisis-photon-sg`, container `arborisis-tiles`, et maintenant les images Docker construites directement sur `arborisis-app-1` sans registre) — dette IaC documentée dans `infra/photon/README.md`.
5. Vérifier le coût réel mensuel de la VM Photon + volume 300 Go dans le Manager Infomaniak (estimation de départ non confirmée précisément, voir décision utilisateur consignée ci-dessous).
6. Alléger l'image Docker (actuellement chaque cible copie l'intégralité de `/repo` avec `node_modules` complet — simplification assumée documentée dans `infra/docker/Dockerfile`, à remplacer par `pnpm deploy --prod` par paquet si la taille d'image devient un problème réel).

**Piège d'environnement à connaître si ça se reproduit** : si des commandes triviales (`grep`, `tsc`, `tsx`) traînent anormalement (minutes au lieu de secondes) sur cette machine, voir plan/MEMORY.md § Infra — probablement `fileproviderd` (iCloud) qui sature l'I/O, pas un bug de ce dépôt. Le contournement qui a fonctionné : lancer `apps/api`/`apps/worker` via `node dist/index.js` (après `tsc -p`) plutôt que `tsx`, qui passe par une couche esbuild plus sensible au symptôme.

## Journal de session

- 2026-08-19 : Mise en place du suivi de tâches, fichiers de gouvernance (LICENSE AGPL-3.0, CONTRIBUTING, CODE_OF_CONDUCT, SECURITY), squelette Terraform Infomaniak complet (non appliqué), squelette Caddy hello-world + docker-compose. Identifiants OpenStack Infomaniak reçus (projet `PCP-RYAEXPT`, régions `dc3-a`/`dc4-a`) et rangés en local hors du repo git (`~/.config/openstack/clouds.yaml`).
- 2026-08-19 : Découverte de 9 VM `github-runner-*` actives sur `dc3-a` (self-hosted GitHub Actions runners, sans rapport avec Arborisis). Suppression confirmée par l'utilisateur et exécutée : 9 instances, 9 IP flottantes, réseau `github-runners-net` + sous-réseau + routeur, security group `github-runners-sg`, keypair `github-runners-key`. Projet cloud vérifié propre après nettoyage.
- 2026-08-19 : Phase 1 démarrée. Monorepo scaffoldé (pnpm workspaces + Turborepo) : `packages/design-tokens` (tokens CSS portés depuis le handoff design), `packages/shared-types` (schémas Zod User/Recording partagés), `packages/db` (schéma Drizzle complet — users/webauthn_credentials/recovery_codes/recordings, migration initiale générée avec l'extension PostGIS). Authentification WebAuthn de bout en bout écrite : `apps/api` (Fastify — register/login start+finish avec `@simplewebauthn/server`, sessions révocables dans Redis, codes de récupération hachés en scrypt, rate limiting) et `apps/web` (Next.js — pages `/register` et `/login` avec `@simplewebauthn/browser`). `apps/worker` (BullMQ) pose la structure du job `publish-recording` avec l'étape Internet Archive en no-op derrière `ARCHIVE_TO_IA=false` (voir mode intérimaire ci-dessus). CI GitHub Actions de base ajoutée (lint/typecheck/test/build avec Postgres+Redis en services). Vérifié réellement en local : `pnpm install`, `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` passent tous. Non vérifié : cérémonie WebAuthn dans un vrai navigateur (Docker/Postgres/Redis indisponibles dans cet environnement d'exécution) — à faire à la prochaine session avant de considérer la Phase 1 entièrement close.
- 2026-08-19 : Phase 1 validée en conditions réelles (Docker lancé par l'utilisateur en cours de session). `docker compose up -d` (Postgres/PostGIS, Redis, Meilisearch) puis `pnpm db:migrate` — deux bugs réels trouvés et corrigés au passage : (1) `packages/db/src/migrate.ts` utilisait `new URL(...).pathname`, qui laisse les espaces du chemin encodés en `%20` et casse la résolution du dossier de migrations sur ce Mac (chemin contenant "Arborisis V2") — corrigé avec `fileURLToPath` ; (2) `drizzle-kit generate` quote à tort le type `geography(Point, 4326)` (sa liste de types PostgreSQL natifs contient "geometry" mais pas "geography") produisant un SQL invalide — corrigé à la main dans la migration générée, piège documenté dans `packages/db/src/columns.ts` pour toute regénération future. Schéma confirmé conforme en base (`\d recordings`, `postgis_version()`). API démarrée contre Postgres/Redis réels : `/health` OK, puis cérémonie WebAuthn complète testée dans un vrai navigateur — inscription (pseudo → passkey → 10 codes de récupération affichés, utilisateur + credential + codes confirmés en base), connexion (session émise, `/auth/me` renvoie l'utilisateur authentifié). Un bug réel trouvé et corrigé pendant ce test : `app.setErrorHandler` enregistré *après* les plugins de routes ne s'appliquait pas aux routes déjà encapsulées par Fastify — une entrée invalide (ex. pseudo trop court) remontait en 500 brut avec le détail Zod complet exposé au client au lieu d'un 400 propre ; corrigé en déplaçant l'enregistrement du handler avant les routes. `pnpm build/typecheck/lint/test` revérifiés verts sur les 6 packages après ces correctifs. **Phase 1 considérée close.**
- 2026-08-19 : Blocage identifié sur Internet Archive — création d'une collection dédiée impossible sans ≈50 items déjà publiés, seuil inatteignable avant lancement. Décision : reporter toute l'intégration IA après le lancement, faire reposer tout le stockage audio (original + proxy) sur l'Object Storage Infomaniak déjà provisionné pendant cette période (mode intérimaire documenté en [05.10](05-stockage-audio-internet-archive.md#510-mode-intérimaire--repli-sur-object-storage-infomaniak-pas-dinternet-archive-au-démarrage), flag `ARCHIVE_TO_IA=false`, sans impact sur le schéma de données ni l'API — juste un no-op réversible). Démarrage de la Phase 1 (socle technique).
- 2026-08-19 : Dépôt GitHub créé et poussé ([0xmagicduck/arborisis](https://github.com/0xmagicduck/arborisis)). `terraform apply` exécuté sur `dc3-a` — VM `arborisis-app-1`, IP flottante `195.15.247.170`, réseau/secgroup dédiés, container Object Storage `arborisis-storage`. Bug rencontré : l'association de l'IP flottante se perdait juste après le premier boot (attribut `network[0].port` instable côté provider OpenStack) — corrigé en réassociant en CLI puis en remplaçant la référence par un data source `openstack_networking_port_v2` (lookup par `device_id`, pas de recréation de la VM). Caddy déployé via SSH/docker compose ; DNS Cloudflare déjà aligné sur la nouvelle IP flottante (coïncidence de récupération de la même adresse par Infomaniak) — certificat Let's Encrypt émis automatiquement. **Chaîne domaine→DNS→VM→TLS validée** : https://arborisis.com répond HTTP/2 200.
- 2026-08-19 : Phase 2 (pipeline d'upload et archivage) implémentée et validée en conditions réelles. Nouveaux packages `@arborisis/storage` (client S3-compat, presign PUT/GET, clés `staging/`·`originals/`·`proxy/`) et `@arborisis/queue` (BullMQ `publish-recording` partagé api/worker, avant dupliqué). API : `POST /uploads/presign` (URL pré-signée, upload staged référencé en Redis par `uploadId`, TTL 1h) et `POST /recordings` (vérifie le dépôt via `HeadObject`, crée l'enregistrement en `processing`, enqueue le job) ; `GET /recordings/:id` (public si `published`, sinon réservé à l'auteur, 404 sans fuite d'existence sinon) et `GET /recordings/mine`. Worker : ffprobe (validation) → ffmpeg (transcodage proxy Opus 128kbps, tranché §5.9) → peaks waveform (décodage PCM brut, 500 buckets) → dépôt Object Storage → `status = 'published'` ; échec définitif après épuisement des 5 tentatives BullMQ → `status = 'failed'` (§5.6), implémenté dans `apps/worker/src/index.ts` sur l'event `failed`. **Décision de schéma** : `original_url`/`streaming_url` renommées en `original_key`/`proxy_key` (migration `0001_rename_recording_storage_keys.sql`, écrite à la main — `drizzle-kit generate` propose un prompt interactif de détection de rename qui ne peut pas s'exécuter en environnement non interactif ici, contourné en générant la snapshot/journal directement) : le container Object Storage est privé, donc l'API stocke des clés et recalcule une URL de lecture pré-signée (1h) à chaque réponse plutôt que de persister une URL qui pourrait expirer ou fuiter.
  - **Identifiants CLI Infomaniak déjà configurés utilisés directement** (à la demande de l'utilisateur, `openstack` CLI avec credentials déjà en place) : génération de credentials EC2 (S3-compatibles) via `openstack ec2 credentials create` pour le container `arborisis-storage` réel — création de secret bloquée par le classificateur de permissions auto, arbitrage demandé et obtenu explicitement de l'utilisateur avant d'exécuter la commande.
  - **Bug réel trouvé et corrigé en testant contre l'Object Storage Infomaniak réel** (pas seulement MinIO) : la passerelle S3-compat d'Infomaniak rejette toute région SigV4 différente de `"us-east-1"` (`AuthorizationHeaderMalformed`), y compris la région OpenStack réelle `dc3-a` qui semblait pourtant correcte — région sans rôle de routage ici (c'est `endpoint` qui fixe la région physique). Corrigé : `OBJECT_STORAGE_REGION` par défaut passé à `us-east-1` partout (`.env.example`, `apps/api/src/config.ts`, `apps/worker/src/config.ts`), documenté dans `packages/storage/src/config.ts`. Testé bout en bout (put/head/URL pré-signée GET/delete) directement contre `arborisis-storage`.
  - **Bug réel trouvé en testant le flux de création d'enregistrement** : `description`/`equipment` étaient `.nullable()` mais pas `.optional()` dans `createRecordingInputSchema` (`@arborisis/shared-types`) — un client omettant la clé (plutôt que d'envoyer explicitement `null`) se faisait rejeter en 400 "Required". Corrigé en rendant ces deux champs `.optional()` spécifiquement sur le schéma d'entrée (le schéma de sortie `recordingSchema` reste inchangé, la clé est toujours présente dans une réponse API).
  - **Bug de fixture de test découvert et corrigé** (pas un bug produit) : la source `sine` de ffmpeg (8.1.2, Homebrew) ne génère pas du plein échelle par défaut (±4095 sur du s16, pas ±32767) — l'assertion `apps/worker/src/__tests__/audio.test.ts` supposait à tort une amplitude proche de 1.0 ; corrigée pour vérifier la cohérence relative des peaks plutôt qu'une valeur absolue liée à un détail d'implémentation du générateur de fixture.
  - **Décision** : ffmpeg/ffprobe résolus via le PATH système (`apps/worker/src/config.ts`, `FFMPEG_PATH`/`FFPROBE_PATH`) plutôt que les paquets npm `ffmpeg-static`/`ffprobe-static` (~80 Mo de binaires téléchargés au postinstall) — CI mise à jour pour installer ffmpeg via apt avant les tests (`.github/workflows/ci.yml`).
  - **Effet de bord constaté** : `docker compose rm -f postgres redis meilisearch` dans ce worktree a fait disparaître les conteneurs `phase-1-project-continue-ce683f-*` d'un autre worktree (Postgres/Redis/Meilisearch, actifs depuis 35 min) — scoping de projet Docker Compose attendu par nom de dossier mais constaté défaillant ici (cause exacte non investiguée : concurrence avec une autre session, ou confusion de projet). Perte de données de dev locales dans cet autre worktree (aucune donnée de production concernée). À surveiller si plusieurs worktrees du même repo tournent `docker compose` en parallèle — signalé à l'utilisateur en session.
  - MinIO ajouté à `docker-compose.yml` (dev local, remplace l'Object Storage Infomaniak, même rôle que Postgres/Redis/Meilisearch déjà en place pour le dev sans dépendance externe). Flux testé de bout en bout via curl contre l'API réelle (Postgres/Redis/MinIO réels, ffmpeg système réel, session injectée directement en Redis faute de cérémonie WebAuthn automatisable en curl) : presign → upload → création → publication → lecture, plus le chemin d'échec (fichier invalide → 5 tentatives → `status = 'failed'`), plus les contrôles d'accès (401 sans session, 404 pour un non-auteur sur un enregistrement non publié). `pnpm build/typecheck/lint/test` verts sur les 9 packages après ces correctifs. **Phase 2 considérée close.**
- 2026-08-20 : Phase 3 (écrans du MVP) implémentée et validée en conditions réelles. Lu intégralement le handoff design (`design/handoff/DEV-HANDOFF.md`) et les 14 mockups `.dc.html` avant tout code, pour porter les valeurs exactes (couleurs, tailles, espacements) plutôt que de les réinventer.

  **Backend — extension minimale nécessaire au frontend** (voir décisions Phase 3 ci-dessus pour le détail) : `GET /recordings` (liste publique, `ILIKE` naïf sur `q`) + jointure `users` sur toutes les routes de lecture (`authorHandle`/`authorDisplayName`), voir `apps/api/src/routes/recordings.ts` et `packages/shared-types/src/recording.ts`.

  **Frontend** : composants partagés (`apps/web/components/`) suivant §2 du handoff — `Header`/`MobileTopBar`/`BottomTabBar` (bascule desktop/mobile 100% CSS, les deux gabarits sont toujours rendus pour éviter tout décalage d'hydratation SSR/CSR), `PlayButton` (cross-fade play/pause, `prefers-reduced-motion` respecté), `Waveform` (barres flex responsives, squelette si `waveformPeaks` est `null`), `RecordingRow`/`RecordingCard` (bascule de layout desktop→mobile via `grid-template-areas` + `display:contents`, pas de composants dupliqués), `ExplorerMap` (placeholder SVG documenté comme tel, voir Phase 4 ci-dessus), `ExplorerSelection`/`RecordingSummaryCard` (panneau flottant desktop + BottomSheet mobile, contenu partagé). Lecteur audio global à instance unique (`apps/web/lib/audio-player.tsx`, un seul `<audio>` pour toute l'app). Polices Public Sans/Source Serif 4 auto-hébergées via `next/font/google` (pas de requête runtime vers Google Fonts).

  **Bugs réels trouvés et corrigés en vérifiant** (voir `plan/MEMORY.md` § Frontend/Next.js pour le détail) :
  - `next build` échouait sur `app/enregistrements/[id]/page.tsx` : en Next 15 App Router, `params` est typé `Promise` même pour un client component — corrigé avec `useParams()` plutôt que la prop `params`.
  - `useSearchParams()` (Explorer, paramètre `?focus=<id>`) sans frontière `<Suspense>` faisait basculer silencieusement toute la page en client-only au build — corrigé.
  - Deux `<button>` imbriqués (composant `IconWithCurrentColor` initial dans `BottomTabBar`, qui clonait des éléments React à la main) : simplifié en passant directement `stroke="currentColor"` dans chaque icône plutôt qu'un clonage fragile.

  **Vérification en conditions réelles** (pas seulement build/typecheck/lint verts, voir §5 de CLAUDE.md) : `docker compose up -d` (Postgres/Redis/MinIO/Meilisearch, isolé par worktree), migration appliquée, base seedée directement en SQL (2 utilisateurs, 6 enregistrements dont un `processing`) pour ne pas dépendre d'une cérémonie WebAuthn non automatisable dans le navigateur de vérification — session injectée directement dans Redis (`session:<id>` → `userId`, cookie `document.cookie` non-`httpOnly` posé côté client puis lu normalement côté serveur, le flag `httpOnly` n'empêchant que la lecture JS, pas l'envoi). `apps/api` et `apps/web` lancés en dev réel, les 6 écrans comparés visuellement aux mockups à 390px (mobile) et desktop dans le navigateur : Explorer (sélection de marqueur → panneau/BottomSheet, bande "Selected", lecture audio togglée), Découvrir (entrée featured 26px vs lignes 16px confirmé via `getComputedStyle`), RecordingDetail (lien mini-carte → Explorer recentré, vérifié bout en bout avec un vrai retry sur erreur réseau transitoire), Recherche (requête debouncée → résultat réel matché sur titre+tag), Profil (grille 3 colonnes desktop confirmée via `getBoundingClientRect`, gabarit Row mobile). **Flux Ajouter exécuté intégralement dans le navigateur** avec un vrai fichier (input `File`/`DataTransfer` simulé en JS, géolocalisation moquée pour contourner l'absence de permission navigateur en environnement headless) : presign réel → upload PUT réel vers MinIO → `POST /recordings` réel → redirection vers la fiche du nouvel enregistrement, retrouvé ensuite dans `/profil` avec le statut "Archiving…" (worker non lancé pendant ce test, comportement attendu).

  **Piège d'environnement rencontré et contourné** (voir `plan/MEMORY.md`) : sourcer le même `.env` pour lancer `apps/api` et `apps/web` fait hériter `next dev` de `PORT=4000`, en conflit avec Fastify ; `tsx watch` est ensuite entré en boucle `EADDRINUSE` sous la charge des redémarrages — contourné en lançant `next dev -p 3000` avec `PORT` explicitement absent de l'environnement, et `apps/api` sans mode `watch` (`npx tsx src/index.ts`) pour la durée de la vérification.

  `pnpm build/typecheck/lint/test` verts sur les 9 packages. **Phase 3 considérée close.**
- 2026-08-20 : Demande initiale de "faire la Phase 4" (carte et recherche). Avant de commencer, question posée à l'utilisateur car Phase 3 semblait, d'après l'état local de ce worktree à ce moment-là, pas commencée côté code. Réponse de l'utilisateur : vérifier l'état réel de Phase 3, et si effectivement seuls login/register manquent d'un vrai front-end, se contenter d'ajouter une tâche à ce sujet. **Correction importante faite en session** : cette branche avait été créée avant qu'une session parallèle ne merge la Phase 3 sur `main` (PR #3, commit `4d9ebf7`) — le constat initial ("Phase 3 pas commencée") était donc obsolète au moment où il a été écrit ici, pas faux dans l'absolu. Détecté seulement quand l'utilisateur a demandé "tu as pull ?" : `git fetch origin` a révélé que `origin/main` avait avancé de plusieurs commits sans qu'aucun pull n'ait été fait avant. `git merge --ff-only origin/main` (après `git stash` de la modif locale de ce fichier) a intégré la Phase 3 réelle, puis conflit résolu à la main pour fusionner le contenu réel de Phase 3 (six écrans ☑, décisions, journal) avec l'unique ajout pertinent de cette session : la ligne « Login / Register — vrai front-end » (☐) dans le tableau Phase 3 ci-dessus — confirmée toujours valable après vérification du diff du commit `4d9ebf7` (ne touche pas à `apps/web/app/{login,register}`). Aucun autre code applicatif modifié cette session.
- 2026-08-20 : Phase 4 (carte et recherche) implémentée. Détail par sujet :
  - **`.pmtiles`** : jar Planetiler officiel (`onthegomap/planetiler`) téléchargé et exécuté en local avec OpenJDK 21 (installé via Homebrew, autorisé par l'utilisateur en session) — génère le schéma **OpenMapTiles**, pas le schéma propriétaire Protomaps (`protomaps/basemaps` est un outil séparé). Décision documentée dans `infra/tiles/README.md`. Extrait Luxembourg choisi pour valider le pipeline complet (téléchargement Geofabrik → jeux de données auxiliaires OpenMapTiles `lake_centerlines`/`water_polygons`/`natural_earth` → génération) sans saturer le peu d'espace disque disponible sur la machine (~16 Go libres au départ). **Bootstrap public Protomaps écarté après test réel** : `demo-bucket.protomaps.com` n'envoie aucun header CORS (vérifié avec `curl -H "Origin: ..."`), inutilisable directement depuis un navigateur — d'où le choix de générer nous-mêmes le premier fichier dès cette session plutôt que documenter une dépendance tierce qui ne marche pas. Script reproductible `infra/tiles/generate.sh` (paramétrable par région Geofabrik), fichier généré non commité (voir `.gitignore`), servi en dev same-origin par Next.js depuis `apps/web/public/tiles/`.
  - **Style MapLibre "Quiet Cartography"** (`apps/web/map-style/quiet-cartography.ts`) : écrit à la main sur le schéma OpenMapTiles, toutes les couleurs dérivées de `--color-ink`/`--color-paper` (aucune couleur hors design system). Décision : **aucune couche de texte/labels** dans cette version (le handoff §3.1 est explicite sur l'absence de labels de rues, et une couche `symbol` exigerait un serveur de glyphs externe supplémentaire non justifié) — y compris le nombre affiché sur un marqueur `cluster`, rendu en HTML/CSS (police du design system) plutôt qu'en glyphes MapLibre, voir point suivant.
  - **`ExplorerMap.tsx`** réécrit intégralement : MapLibre GL JS + protocole `pmtiles://` (package `pmtiles`), marqueurs rendus en `maplibregl.Marker` HTML (pas en couche native `circle`/`symbol`) pilotés par une vraie instance **Supercluster** côté client, pour reproduire exactement les 3 variantes `default`/`selected`/`cluster` du handoff §2.4 (rayons/couleurs exacts) sans dépendance à un serveur de glyphes pour le chiffre du cluster. Chargement des marqueurs via un nouvel endpoint `GET /recordings/viewport` (PostGIS `ST_MakeEnvelope` + `&&` sur l'index GIST déjà existant, ne renvoie que id/titre/coordonnées) rechargé sur `moveend`. Sélection d'un marqueur → requête ciblée `GET /recordings/:id` existante (nouveau hook `useRecording`) plutôt que de dépendre de la liste générale déjà chargée, pour rester correct même pour un marqueur hors du lot initial. Attribution CC-BY ("© OpenMapTiles © OpenStreetMap contributors") câblée via `AttributionControl`, obligation de licence.
  - **Géocodage Photon** (`apps/web/lib/geocoding.ts`) : instance publique bootstrap, CORS vérifié en conditions réelles (`curl -H "Origin: http://localhost:3000" https://photon.komoot.io/api/...` → `Access-Control-Allow-Origin: *`). Autocomplete débouncé câblé dans `StepDetails.tsx`, en complément du bouton "Use my current location" existant (gardé pour les lieux hors sentiers absents d'OSM) plutôt qu'en remplacement.
  - **Meilisearch** : nouveau package `@arborisis/search` (client, réglages d'index idempotents, recherche+facettes). Indexation à la publication ajoutée dans `apps/worker/src/jobs/publish-recording.ts` (après le passage en `status = 'published'`). Nouvel endpoint `GET /recordings/search` (facettes tags/license/locationLabel, requête DB de ré-hydratation qui préserve l'ordre de pertinence Meilisearch). Écran Recherche réécrit : filtres "location/tag/duration" du mockup désormais fonctionnels (menus déroulants listant les facettes réelles, tranches de durée choisies arbitrairement en attendant une décision produit — voir plan/08 §8.5) plutôt qu'inertes.
  - **Bug réel trouvé et corrigé** : encodage des paramètres `tags`/`license`/`locationLabel` en query string — `locationLabel` contient lui-même une virgule ("Lieu, Pays"), un simple `join(",")`/`split(",")` aurait donc mal découpé la valeur ; corrigé en `encodeURIComponent`-échappant chaque élément avant la jonction (`packages/shared-types/src/recording.ts`, `apps/web/lib/api.ts`).
  - **Bugs réels trouvés par `pnpm typecheck`** (voir plan/MEMORY.md pour le détail) : décoration Fastify nommée `search` en collision de type avec la méthode HTTP native `fastify.search()` (renommée `fastify.meilisearch`) ; deux cas `noUncheckedIndexedAccess` dans `ExplorerMap.tsx` (déstructuration de `Position` GeoJSON, assignation de classe CSS Modules à `element.className` DOM natif).
  - **Vérification en conditions réelles interrompue par un incident d'environnement, pas terminée cette session** : `pnpm typecheck` vert sur les 3 apps + les packages touchés (confirmé, mais chaque `tsc`/`eslint` a mis exceptionnellement 2 à 35 minutes au lieu de secondes). Un reboot en cours de session (demandé par l'utilisateur pour tenter de résoudre un premier ralentissement) a laissé `fileproviderd` (le process iCloud Drive — le dépôt vit dans `~/Library/Mobile Documents/com~apple~CloudDocs/...`) saturer l'I/O pendant plus d'une heure, avec des lectures de fichiers dans `node_modules` intermittemment vides/tronquées (`npx tsx src/index.ts` plantait avec `TypeError: r.register is not a function`, un fichier `.cjs` de `tsx` lu vide). L'utilisateur a basculé le dossier en "toujours garder sur ce Mac" en cours de session pour limiter le phénomène ; la démarche release build (`tsc -p`, pour lancer l'API compilée via `node` plutôt que `tsx`/esbuild) était encore en cours à la fin de cette session. Seed SQL de vérification préparé (6 enregistrements, dont un `processing` et un hors couverture tuiles) et appliqué en base ; 5 documents poussés et testés directement dans Meilisearch via `curl` (index/réglages/recherche/facettes fonctionnels, validé hors API) — mais **aucune vérification via le navigateur ou via l'API/le worker réels n'a pu être complétée**, contrairement à la culture habituelle de ce dépôt (§5 CLAUDE.md). Phase 4 **non considérée close** : voir "Prochaine session" pour la suite exacte.
  - Une session Claude Code parallèle (`main`, consigne "continuer la Phase 4") a été contactée en cours de session pour éviter un travail dupliqué/des conflits de merge — accord pour la laisser attendre la fin de cette branche.
  - **Suite et clôture, même session** : la build `tsc -p` d'`apps/api` a fini par aboutir ; lancé via `node dist/index.js` (contourne `tsx`/esbuild, la couche qui coinçait spécifiquement — `tsc --noEmit`/`eslint` fonctionnaient déjà, seul `tsx` restait bloqué) → API up en quelques secondes. Même traitement pour `apps/worker`. `apps/web` : `next build` échouait avec `Error: <Html> should not be imported outside of pages/_document` sur `/404` — bisecté via `git stash`/rebuild (Phase 3 seule buildait proprement, 10/10 pages statiques) pour confirmer que la régression venait bien des changements Phase 4, puis identifié : `maplibre-gl` importé statiquement dans `page.tsx` plantait le rendu SSR que Next tente même pour une page `"use client"` lors de la génération statique — corrigé avec `next/dynamic({ ssr: false })` (voir plan/MEMORY.md). Un second bug CSS découvert au premier chargement réel : la carte restait confinée à ~67px de haut (au lieu de remplir `.mapArea`) à cause d'une ambiguïté `height: 100%` sur un enfant statique d'un conteneur à hauteur `flex: 1` — corrigé en `position: absolute; inset: 0` (voir plan/MEMORY.md).
  - **Vérification complète en conditions réelles, dans un vrai navigateur, contre les 3 process réels (API/worker/web) et Postgres/Redis/Meilisearch/MinIO réels** : Explorer affiche la vraie carte Luxembourg (style "Quiet Cartography" conforme, attribution CC-BY visible), clustering réel — bulle "3" pour 3 enregistrements proches, clic → zoom in → re-clustering en "2" + 1 marqueur isolé, clic sur un marqueur individuel → panneau de sélection peuplé via une vraie requête `GET /recordings/:id` (titre/lieu/auteur/durée corrects). Recherche : menu déroulant "tag" listant les vraies facettes Meilisearch (birds/forest/wind/dawn/insects/night/stream/urban avec comptages), filtre "birds" appliqué → "2 recordings found" avec les bons résultats. Géocodage Photon : requête réelle depuis l'origine `localhost:3000` confirmée (CORS + résultats corrects, dont "Forêt de Soignes/Sonian Forest, Belgique" — le lieu même du seed). `pnpm build/typecheck` verts. **Phase 4 considérée close.**
- 2026-08-20 : Phase 5 (durcissement avant lancement) traitée dans son intégralité — les 6 tâches du tableau ci-dessus. `git fetch` confirmé à jour avec `origin/main` avant de démarrer (aucun retard cette fois). Deux décisions posées avec l'utilisateur en tout début de session (voir ci-dessous) avant de toucher à l'infra réelle, car consommant du budget récurrent réel.

  **Décisions posées avec l'utilisateur avant d'agir** :
  - **Identité légale** pour les pages CGU/confidentialité/mentions légales : Bastien Javaux, rue du Craetveld 135, 1120 Neder-over-Heembeek (Belgique), `bastienjavaux@icloud.com` — fournie explicitement par l'utilisateur, utilisée telle quelle (pas de placeholder).
  - **Géocodage auto-hébergé** : l'utilisateur a d'abord demandé "redimensionne le disque à 300G et fais l'index complet" (planète). Clarifié qu'aucun gabarit Infomaniak n'offre 300 Go de disque racine — un volume Cinder séparé de 300 Go a été provisionné à la place (réversible, détachable indépendamment). Puis découverte que l'index planète recommande **64 Go de RAM** (documentation Photon), incompatible avec la VM de prod (8 Go, partagés avec tout le reste de la stack — risque réel de faire tomber le site) : question reposée à l'utilisateur, qui a choisi une **VM dédiée séparée** (retenu : 32 Go RAM, compromis coût/marge — voir `infra/photon/README.md`).

  **Sécurité** : `pnpm audit --prod` a trouvé 6 vulnérabilités réelles, dont une **haute directement dans une dépendance de premier niveau** (`drizzle-orm` < 0.45.2, injection SQL via identifiants mal échappés — GHSA-gpj5-g38j-94v9). Bump `drizzle-orm` 0.36.4→0.45.2 et `drizzle-kit` 0.28.1→0.31.10 (packages/db, apps/api, apps/worker), overrides `pnpm` pour forcer `sharp`/`postcss` patchés (dépendances transitives de Next.js). **`pnpm audit` repasse à `0 vulnerabilities found`.** Revérifié à fond avant de considérer le bump sûr : `pnpm db:generate` (aucun changement de schéma inattendu), `pnpm build/typecheck/test`, et surtout une requête réelle contre Postgres (`GET /recordings`, qui utilise `sql\`ST_X/ST_Y\`` — la zone la plus susceptible de casser avec un bump majeur de l'ORM) exécutée avec succès contre les données seedées. `@fastify/helmet` ajouté à l'API (CSP désactivée côté API, pure JSON — la CSP réelle vit dans le Caddyfile de prod qui sert le HTML). En-têtes confirmés présents sur une vraie réponse `/health`. Rate limiting auth déjà en place depuis Phase 1, pas de régression.

  **Modération** : nouvelle table `reports` (migration `0002_flaky_black_bird.sql`, appliquée avec succès, pas de piège `geography` cette fois — table sans colonne géographique). Décision : pas de rôle admin en base — un utilisateur est "admin" si son handle figure dans `ADMIN_HANDLES` (env), suffisant pour qu'une revue manuelle légère (§10.3) soit réellement possible sans construire un système de rôles complet hors scope MVP (voir `apps/api/src/lib/admin.ts`). Bouton "Report this recording" ajouté à `RecordingDetail` (aucun mockup design/system pour cet écran, comme Login/Register — composé à partir des primitives `Button`/`FormField` existantes). **Vérifié de bout en bout dans un vrai navigateur** avec une session Redis injectée (pattern Phase 3) : signalement refusé proprement si non connecté (message + lien `/login`), déposé avec succès une fois connecté, lu et résolu via `GET`/`PATCH /reports` en tant qu'utilisateur listé dans `ADMIN_HANDLES`, rejeté en 403 pour un utilisateur non listé, doublon volontairement idempotent (pas d'erreur, pas de second enregistrement — contrainte unique `(recording_id, reporter_id)`).

  **Accessibilité (WCAG AA)** : audité contre la checklist déjà écrite en DEV-HANDOFF §7 (pas réécrite de zéro) — calcul réel du ratio de contraste (formule de luminance relative WCAG) de `--color-stone` sur `--color-paper` : **~3.3:1, sous le seuil AA de 4.5:1** pour du texte normal. Plutôt que de changer le token globalement (décision de design system qui aurait besoin d'une validation séparée, et `--color-ink-faint`/`--color-stone` sur les durées est une décision produit déjà documentée dans `tokens.css`, pas un oubli), corrigé texte par texte : tout usage de `--color-stone` portant une information unique et non redondante (navigation desktop, back-links, auteur/date, compteurs, liens de pied de page, labels de formulaire, messages de statut/erreur) basculé vers `--color-ink-secondary` (déjà ~6.6:1, déjà le token prescrit par le handoff lui-même pour ce cas). Laissé en l'état : les micro-labels redondants avec une icône ou un contexte visuel (BottomTabBar, eyebrows de section), et les durées (décision produit explicite). Cibles tactiles 44px ajoutées sur deux radiogroups qui n'en avaient pas (licence dans le flux Ajouter, raison de signalement). **Incident rencontré et contourné** : plusieurs éditions de fichiers CSS coup sur coup pendant que `next dev` tournait ont fait dériver le cache HMR (`Cannot find module './NNN.js'`, `__webpack_modules__[moduleId] is not a function`) — pas un bug de code, corrigé par un restart propre (`rm -rf .next` + relance), reproduit deux fois pendant la session.

  **Sauvegardes** : nouveau package `@arborisis/backup` (`pg_dump --format=custom` → GPG symétrique AES256 → Object Storage, clé passphrase jamais en argument de ligne de commande — fichier temporaire `chmod 600` supprimé immédiatement après usage). Rétention 30 jours glissants (`daily/`) + 12 mensuelles (`monthly/`, une copie à la première sauvegarde du mois). Script de restauration exige explicitement `RESTORE_TARGET_DATABASE_URL` et refuse si elle vaut `DATABASE_URL` — sécurité contre une restauration accidentelle sur la prod. **Testé en conditions réelles contre l'Object Storage Infomaniak réel** (`arborisis-storage`, credentials EC2 déjà générés en Phase 2) : sauvegarde réelle déposée (`backups/postgres/daily/...` et `backups/postgres/monthly/2026-08...`), confirmée via `openstack object list`, puis restaurée dans une base PostgreSQL/PostGIS fraîche (`arborisis_restore_drill`) — comptages de lignes (`users`/`recordings`/`webauthn_credentials`/`reports`) identiques à la source. **Bug réel trouvé et corrigé** : `pg_dump`/`pg_restore` installés via `brew install libpq` suivent la dernière version majeure (18.6) alors que le serveur cible est en 16.4 — le dump généré contient `SET transaction_timeout = 0` (directive PG17+) que le serveur 16 ne reconnaît pas, faisant échouer la restauration (`unrecognized configuration parameter`). Corrigé en installant `postgresql@16` en parallèle et en pointant explicitement dessus. Documenté dans `plan/MEMORY.md`.

  **Pages légales** : CGU/confidentialité/mentions légales rédigées avec l'identité réelle donnée par l'utilisateur (voir décisions ci-dessus), reflètent l'état réel du traitement de données (§10.2/§10.5 déjà écrits dans le plan, portés fidèlement) — pas d'email/mot de passe collecté, Internet Archive explicitement présenté comme "pas encore actif" plutôt que comme si l'intégration existait déjà. 3 nouvelles routes (`/mentions-legales`, `/confidentialite`, `/cgu`) via un composant `LegalPage` partagé, liées depuis `/profil` plutôt qu'un footer global commun à `AppShell` — décision délibérée : `AppShell` est partagé par Explorer, dont la carte plein cadre repose sur un calcul de hauteur flex documenté comme fragile dans `plan/MEMORY.md`, un footer global aurait risqué de le recasser pour un gain mineur.

  **Auto-hébergement tuiles + géocodage** (le chantier infra le plus lourd de cette session, utilisant `openstack` CLI directement sur le projet réel `PCP-RYAEXPT`) :
  - **Géocodage** : VM dédiée `arborisis-photon-1` provisionnée (`a8-ram32-disk20-perf1`, réseau privé `arborisis-net` partagé avec `arborisis-app-1`, aucune IP publique sur le port 2322 — sécurité : Photon est un service de recherche non-authentifié, l'exposer publiquement en ferait un service gratuit exploitable par n'importe qui). Volume `arborisis-photon-data` (300 Go Cinder) attaché et monté. Index **planète complet** téléchargé depuis la publication officielle komoot (~58 Go compressé, ~90 Go extrait, ~35 minutes à ~20-350 Mbps) et servi via un service systemd (Photon 1.3.0, backend OpenSearch interne, `-Xmx20g`). **Vérifié avec des requêtes réelles à couverture planétaire** (Paris, Tokyo, Forêt de Soignes — pas seulement l'extrait Luxembourg du `.pmtiles`). Le navigateur n'appelle plus jamais Photon directement (contrairement au bootstrap Phase 4) : nouveau `GET /geocode` côté API (`apps/api/src/routes/geocode.ts`) proxifie vers `PHOTON_URL` (variable serveur, pas `NEXT_PUBLIC_*`), `apps/web/lib/geocoding.ts` mis à jour en conséquence — vérifié par un vrai appel `fetch` depuis un navigateur réel vers l'API réelle. **Bug réel trouvé et corrigé** : la première tentative depuis `arborisis-app-1` vers `192.168.120.209:2322` a timeout malgré un security group Neutron correctement configuré — cause réelle : `ufw` (activé par le `cloud-init.yaml` réutilisé pour cette VM) n'ouvrait par défaut que 22/80/443, pas 2322 — deux couches de pare-feu indépendantes, la seconde oubliée. Corrigé (`ufw allow from 192.168.120.0/24 to any port 2322 proto tcp`, règles 80/443 inutiles retirées). Détail complet dans `infra/photon/README.md`.
  - **Tuiles** : nouveau container Object Storage public `arborisis-tiles` (ACL de lecture publique activée par l'utilisateur lui-même via le Manager Infomaniak après un premier essai infructueux en CLI — la propriété `X-Container-Read` posée via `openstack container set --property` n'avait aucun effet observable, contrairement au réglage fait depuis le Manager). `luxembourg.pmtiles` (35,8 Mo) déposé, Range requests HTTP confirmées (`206 Partial Content` sur une vraie requête `Range`). **CORS indisponible côté Infomaniak** — testé et confirmé par deux voies distinctes : l'ACL Swift native (`X-Container-Meta-Access-Control-Allow-Origin`, posée via CLI, sans effet observable sur la réponse HTTP réelle) et l'API S3-compatible (`PutBucketCorsCommand`, qui répond `501 NotImplemented`). Contourné en faisant de Caddy un proxy same-origin (`handle_path /tiles/*` → réécriture vers le chemin Swift complet → `reverse_proxy` vers `s3.pub1.infomaniak.cloud`) — same-origin élimine le problème CORS par construction, tout en supportant nativement les `Range` requests. **Syntaxe validée** (`caddy validate`) et **testée en conditions réelles** avec une vraie instance Caddy locale (Docker, écoute `:8080`) interrogeant le vrai bucket — `206 Partial Content` correct reçu de bout en bout.
  - **Dette IaC assumée** : toutes ces ressources (VM Photon, volume, security group, container tiles) créées via `openstack` CLI directement, pas via Terraform — pas d'état Terraform local disponible dans ce worktree pour un `apply` sûr sans risquer de recréer/détruire les ressources existantes (VM de prod, réseau, container principal) déjà appliquées lors d'une session précédente sur une autre machine. Documenté comme dette explicite dans `infra/photon/README.md`, à importer (`terraform import`) le jour où un backend Terraform distant partagé existe (voir `infra/terraform/versions.tf`).

  `pnpm build/typecheck/lint/test` verts sur les 14 packages (dont les 2 nouveaux : `@arborisis/backup`, plus les routes/composants ajoutés aux apps existantes) après tous ces changements. **Phase 5 considérée close** — reste ouvert : déploiement réel de l'app sur `arborisis-app-1` (toujours hello-world, voir Phase 6), import Terraform de la dette IaC, vérification du coût réel de la VM Photon + volume dans le Manager Infomaniak.
- 2026-08-20 : Tâche indépendante "Login/Register — vrai front-end" close. Nouveau gabarit partagé `apps/web/components/AuthPage.{tsx,module.css}` (aucun mockup `.dc.html` n'existe pour ces deux écrans — même situation que `LegalPage` en Phase 5, dont le patron a été repris : colonne étroite centrée, titre serif italique, lien "← Explorer"). `app/login/page.tsx` et `app/register/page.tsx` réécrits contre les composants partagés `Button`/`FormField` (`TextField`) au lieu du HTML brut à style inline de la Phase 1 ; logique WebAuthn (`@simplewebauthn/browser`) inchangée, ajout de `useSession().refresh()` après connexion/inscription réussie pour que le header bascule immédiatement en état authentifié. Écran des 10 codes de récupération restylé en grille 2 colonnes (police sans-serif avec letter-spacing plutôt qu'une police monospace dédiée : aucun token `--font-mono` n'existe dans le design system fermé, voir `packages/design-tokens/src/tokens.css`). `tsc --noEmit` et `eslint` verts. **Vérifié en conditions réelles dans un vrai navigateur** contre l'API/Postgres/Redis réels de ce worktree (`docker compose` déjà actifs, API lancée via `npx tsx src/index.ts` avec `.env` sourcé séparément — piège déjà documenté en §6 CLAUDE.md) : cycle d'inscription complet exécuté (pseudo → cérémonie WebAuthn → 10 codes affichés → redirection Explorer authentifié), déconnexion, puis reconnexion avec le même compte — les deux cycles aboutissent, avatar de header confirmé passant à l'état connecté après chacun. Rendu comparé visuellement à desktop (1280px, header nav complet) et mobile (< 768px, header "back") aux autres écrans de l'app. Compte de test (`verif-visuel-test`) supprimé de la base après vérification.
- 2026-08-20 : **Phase 6 — premier déploiement réel de l'application sur `arborisis-app-1`.** Nouveau `infra/docker/Dockerfile` (multi-cible : `api`/`worker`/`web`/`backup`, un seul stage `build` partagé pour le monorepo pnpm/Turborepo) et `infra/docker-compose.yml` étendu avec `postgres`/`redis`/`meilisearch`/`api`/`worker`/`web`/`backup` (service `migrate` à profil `tools` pour les migrations ponctuelles). `infra/README.md` créé, documentant les variables d'environnement de prod et la procédure de déploiement. Déployé via PR successives (#6 à #9) mergées sur `main`, puis `git clone`/`pull` réel sur `arborisis-app-1` (accès SSH réel confirmé, clé `~/.ssh/arborisis_ed25519`). Credentials Object Storage réutilisées (EC2 credentials déjà existantes du projet OpenStack, mêmes que Phase 2/5) ; `POSTGRES_PASSWORD`/`MEILI_MASTER_KEY`/`BACKUP_GPG_PASSPHRASE` générés (`openssl rand`) ; `ADMIN_HANDLES=san2stic` posé sur demande explicite de l'utilisateur.
  - **Quatre bugs réels trouvés et corrigés pendant ce déploiement** (détail dans `plan/MEMORY.md` § Déploiement/Docker) : (1) `POSTGRES_PASSWORD` généré en base64 contenait un `/`, cassant le parsing de la connection string Postgres (`ERR_INVALID_URL` peu explicite) — régénéré en hex, volume Postgres recréé (base vide à ce stade, aucune perte) ; (2) le service `worker` manquait l'override `MEILI_URL: http://meilisearch:7700` (présent seulement sur `api`), crash-loop `ECONNREFUSED ::1:7700` sur le défaut dev ; (3) `NEXT_PUBLIC_API_URL`/`NEXT_PUBLIC_PMTILES_URL` posées seulement en variable d'environnement runtime du conteneur `web` — sans effet, `next build` les inline au moment de la build : passées en build-arg Docker à la place ; (4) **CSP `script-src 'self'` cassait le rendu de toute page en production** (page blanche, Next.js App Router injecte un `<script>` inline par page pour l'hydratation RSC) — jamais vérifiée en conditions réelles avant cette session (l'app n'avait jamais tourné derrière Caddy), corrigée en urgence avec `'unsafe-inline'` directement sur le serveur puis commitée, une CSP à base de nonce reste à faire proprement plus tard.
  - **Vérifié en conditions réelles sur le domaine public `https://arborisis.com`** (pas seulement en interne) : migrations Drizzle appliquées contre le Postgres/PostGIS de prod (`\dt` confirme les tables applicatives), cycle d'inscription WebAuthn complet exécuté dans un vrai navigateur contre `/register` (passkey → 10 codes de récupération → session), `GET /api/auth/me` (401 non connecté), `GET /api/recordings`, `GET /geocode?q=Paris` (proxy vers l'instance Photon privée) tous répondent réellement, tuiles `.pmtiles` servies via le proxy Caddy same-origin (`206 Partial Content` confirmé sur le vrai bucket `arborisis-tiles`). Compte de test (`prodcheck`) supprimé après vérification.
  - **Sauvegardes activées** : service `backup` câblé dans `infra/docker-compose.yml` (n'était pas fait avant cette session, voir `infra/backup/README.md`), timer systemd `arborisis-backup.timer` (03:00 UTC nocturne, `Persistent=true`) installé et activé, **testé en exécution manuelle réelle** (`systemctl start arborisis-backup.service` → `pg_dump` → chiffrement GPG → dépôt réel sur `arborisis-storage/backups/postgres/daily/...` confirmé dans les logs).
  - **Non fait cette session** : annonce du dépôt GitHub (action de publication distincte, non demandée), surveillance Internet Archive (sans objet, intégration reportée), CSP à base de nonce (contournée en urgence avec `unsafe-inline`), import Terraform des ressources de déploiement (images Docker construites directement via `docker compose build` sur le serveur, pas de registre d'images ni d'IaC pour ce déploiement).
- 2026-08-20 : **Emprise géographique de production du `.pmtiles` tranchée : Europe** (demande explicite de l'utilisateur — la planète entière a été écartée après discussion sur le coût/temps d'une VM temporaire dédiée, une emprise plus modeste que l'Europe n'a pas été retenue non plus). Génération lancée en tâche de fond sur `arborisis-photon-1` (32 Go RAM dont ~1,4 Go réellement utilisés par le service Photon en prod, 190 Go libres — VM déjà provisionnée réutilisée plutôt qu'une nouvelle VM temporaire chez Infomaniak, à la suggestion de l'utilisateur, aucun coût d'infra additionnel). Script adapté de `infra/tiles/generate.sh` (`-Xmx16g`, `nice -n10`/`ionice -c2 -n7` pour ne pas affamer le service Photon en prod tournant en parallèle sur la même VM), lancé via `setsid nohup ... & disown` en SSH — **toujours en cours à la fin de cette session** (osm_pass1 ~13 min après le lancement, ~3,8 milliards de nœuds/45 Go traités, débit stable), voir "Prochaine session" pour la suite exacte (dépôt Object Storage, bascule `NEXT_PUBLIC_PMTILES_URL`, rebuild `web`).
- 2026-08-20 : **Deux bugs réels remontés en direct par l'utilisateur le lendemain du lancement, corrigés et déployés** : (1) prévisualisation audio locale avant upload bloquée par la CSP (`media-src` sans `blob:`, `URL.createObjectURL` utilisé par `StepDetails.tsx`) ; (2) upload complètement cassé (préflight CORS en `405` sur le `PUT` pré-signé vers la passerelle S3-compat Infomaniak, jamais fiable pour CORS d'après l'expérience Phase 5 sur les tuiles). Corrigés dans [PR #11](https://github.com/0xmagicduck/arborisis/pull/11) (mergée par l'utilisateur) : `media-src blob:` ajouté ; upload proxifié same-origin via Caddy (`handle_path /storage-upload/*` + `OBJECT_STORAGE_UPLOAD_PROXY_URL`), même stratégie déjà éprouvée pour `/tiles/*`. **Déployé et vérifié en conditions réelles** : `git pull` + `docker compose build api web` + `up -d` sur `arborisis-app-1` (exécuté par l'utilisateur, accès SSH en écriture refusé à l'agent par le classificateur de permissions sur cette VM spécifiquement — lecture seule autorisée), `curl -I https://arborisis.com/` confirmant la nouvelle CSP. **Bug de déploiement supplémentaire trouvé et corrigé pendant cette vérification** : `docker compose exec caddy caddy reload` rechargeait l'ancien contenu du Caddyfile (`"msg":"config is unchanged"` dans les logs Caddy) malgré le `git pull` — le bind mount Docker était lié à l'ancien inode du fichier (remplacé par un rename atomique via `git pull`, pas une édition en place). Corrigé avec `docker compose up -d --force-recreate caddy`, qui ré-établit le bind mount ; nouvelle CSP confirmée en prod après coup. Détail complet dans `plan/MEMORY.md` § Déploiement/Docker.
